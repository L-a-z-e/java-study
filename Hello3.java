public class Hello3 {
    //필드 정의
    int age;
    //int -> 키워드
    //age -> 식별자(변수)
    int myAge = 10;

    //메소드(함수)

    //두개의 숫자값을 입력받아서 높은 값을 구하는 메소드

    //매개변수 (필수는 아니다.)
    //리턴타입 (필수는 아니다.)
    int maxNumber(int x, int y)
    {
        return 0;
    }
    // 리턴타입 함수명(매개변수)

    int maxNumber(){
        return 0;
    }
    //매개변수 x

    void maxnumber(){
        int result = maxNumber(10,20);
    }
    // 리턴타입 , 매개변수 x
}
